import type{Ticket}from'../domain/ticket.js';import type{TicketRepository}from'./ticketRepository.js';const seed:Ticket[]=[{id:'TKT-1842',subject:'Card payment reversed twice',customer:'Northwind Foods',priority:'urgent',status:'in_progress',assignee:'Rahul Maurya',createdAt:new Date(Date.now()-46*60*60*1000).toISOString(),updatedAt:new Date().toISOString()},{id:'TKT-1841',subject:'Unable to export monthly statement',customer:'Acme Logistics',priority:'high',status:'open',createdAt:new Date(Date.now()-3*60*60*1000).toISOString(),updatedAt:new Date().toISOString()},{id:'TKT-1837',subject:'Add user to billing workspace',customer:'Pine Labs',priority:'medium',status:'waiting_customer',assignee:'J. Singh',createdAt:new Date(Date.now()-8*60*60*1000).toISOString(),updatedAt:new Date().toISOString()}];export class MemoryTicketRepository implements TicketRepository{private items=[...seed];async list(){return[...this.items].sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt))}async get(id:string){return this.items.find(x=>x.id===id)}async save(ticket:Ticket){const i=this.items.findIndex(x=>x.id===ticket.id);if(i>=0)this.items[i]=ticket;return ticket}}
