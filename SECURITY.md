# Security

This repository is a portfolio project and does not process real customer data. Please do not open public issues containing credentials, tokens, personal information, or production data.

For a real deployment I would use a managed secret store, short-lived credentials, TLS at the edge, dependency scanning, and environment-specific access controls. Demo values in `.env.example` are intentionally non-production placeholders.
